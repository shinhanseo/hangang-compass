# AGENTS.md

이 저장소에서 작업하는 모든 Codex 에이전트는 이 문서를 따른다. 제품 기준은 `PRODUCT.md`, 사용자 검수 기준은 `docs/ACCEPTANCE.md`를 우선한다.

## 1. 작업 방식

- 사용자는 구현자가 아니라 제품 결과의 검수자다. 구현, 테스트, 문서화, 반복 수정은 Codex가 담당한다.
- 작업 전 관련 문서와 현재 변경 상태를 확인한다. 확인하지 않은 사실을 추측해 구현하지 않는다.
- 한 번에 하나의 작고 검증 가능한 결과를 만든다. 대규모 골격 생성보다 수직으로 동작하는 얇은 단위를 선호한다.
- 기능 변경에는 그 기능을 증명하는 자동 검증을 함께 추가한다.
- 중요한 제품 결정, 외부 비용 발생, 개인정보 범위 확대, 되돌리기 어려운 변경은 사용자에게 선택지와 영향을 제시해 검수받는다.
- 불명확하지만 안전하게 되돌릴 수 있는 구현 세부사항은 합리적인 가정을 문서화하고 진행한다.
- 범위 밖 기능을 “완성도” 명목으로 추가하지 않는다.

## 2. 구현 순서

1. 위험한 가정을 가장 작은 스파이크로 검증한다.
2. 실패·누락·오래된 데이터의 동작을 먼저 정의한다.
3. 도메인 규칙과 외부 서비스 경계를 분리한다.
4. 자동 테스트를 추가한다.
5. 사용자 검수 시나리오를 통과시킨다.
6. 결과와 남은 위험을 문서화한다.

UI 구현보다 데이터 접근 가능성, 경로 품질, 후보 만남 지점, 추천 규칙의 재현성을 먼저 검증한다.

## 3. 검증 원칙

- 완료를 주장하기 전에 관련 테스트, 정적 분석, 빌드 또는 실행 검증을 수행한다.
- 실행하지 못한 검증은 통과했다고 표현하지 않고 이유와 영향을 보고한다.
- 추천 알고리즘은 고정 입력으로 결과와 설명 근거를 재현하는 테스트를 가져야 한다.
- 외부 API 연동은 성공뿐 아니라 시간 초과, 호출 제한, 빈 응답, 오래된 응답, 일부 소스 장애를 테스트한다.
- 화면 변경은 핵심 상태(로딩, 성공, 부분 데이터, 실패, 빈 상태)를 검수 가능하게 만든다.
- 실제 자격증명에 의존하지 않는 fixture/fake를 기본 테스트 경로로 둔다.

## 4. 사용자 검수 지점

다음 시점에는 구현 결과와 함께 짧고 구체적인 검수 자료를 제시한다.

- 기술 스택과 유료 외부 API를 확정하기 전
- 추천 점수, 제외 조건, 공평성 정의를 확정하기 전
- 위치·약속 데이터의 보관 정책을 확정하기 전
- 첫 클릭 가능한 핵심 여정이 준비됐을 때
- MVP 배포 후보가 인수 기준을 모두 통과했을 때

검수 자료에는 확인 방법, 기대 결과, 알려진 제한, 결정이 필요한 항목만 포함한다. 코드 지식이 없어도 수행할 수 있어야 한다.

## 5. 데이터와 추천 규칙

- 공식 문서나 실제 응답으로 확인하지 않은 필드, 갱신주기, 정확도, 비용, 약관을 사실처럼 쓰지 않는다.
- 외부 원본을 내부 정규 모델로 변환하고, 추천 도메인이 공급자별 응답 형식에 직접 의존하지 않게 한다.
- 모든 외부 데이터에 출처, 조회 시각, 관측/예측 대상 시각, 신뢰 상태를 보존한다.
- 점수와 설명은 동일한 계산 근거에서 나온다. 설명용 숫자를 별도로 꾸며내지 않는다.
- 데이터 누락 시의 감점, 제외, 대체 규칙을 명시하고 테스트한다.
- 생성형 AI를 추천 결정의 필수 경로에 넣지 않는다.

## 6. 비밀키·공공 API 안전 규칙

- API 키, 토큰, 개인 위치, 실제 약속 링크를 저장소·로그·스크린샷·fixture에 커밋하지 않는다.
- 비밀값은 환경변수 또는 승인된 비밀 저장소로만 주입하고 `.env.example`에는 이름과 설명만 둔다.
- 클라이언트 번들에 비밀키를 넣지 않는다. 공급자가 공개 클라이언트 키를 명시적으로 허용하는 경우에도 도메인/앱 제한을 적용한다.
- 공공데이터도 호출 한도, 이용약관, 재배포·캐시 허용 범위, 출처 표기 조건을 확인한다.
- 로그에는 출발지 원문 대신 필요 최소한의 비식별 식별자나 격자화된 값을 사용한다.
- API 응답 원본을 장기 보관하지 않는다. 보관이 필요하면 목적, 기간, 삭제 방식을 먼저 문서화한다.
- 키 유출이 의심되면 즉시 사용을 중단하고 사용자에게 폐기·재발급 필요성을 알린다.

## 7. 변경 관리

- 기존 사용자 변경을 덮어쓰거나 되돌리지 않는다.
- 문서와 구현이 어긋나면 같은 변경에서 함께 갱신한다.
- 새 의존성은 필요성, 유지보수 상태, 라이선스, 크기, 대안을 확인한다.
- 포매터에 의한 무관한 대량 변경을 피한다.
- 커밋 또는 PR을 요청받은 경우 목적이 하나로 읽히는 단위로 구성한다.

## 8. 완료 정의

작업은 다음 조건을 모두 만족할 때 완료다.

- 요청된 사용자 결과가 동작한다.
- 관련 인수 기준과 자동 검증이 통과한다.
- 실패 및 부분 데이터 상태가 정의되고 검증됐다.
- 접근성, 개인정보, 비밀키 위험을 점검했다.
- 관련 제품·아키텍처·운영 문서가 최신이다.
- 사용자가 코드 없이 확인할 검수 방법이 제공됐다.
- 실행하지 못한 항목과 남은 위험이 명시됐다.
